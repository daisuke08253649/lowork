# AGENTS.md

> このファイルはCodex（実装担当）とClaude Code（レビュー担当）が本プロジェクトで一貫した判断を行うための運用ルールです。
> 要件・設計の詳細は重複記載せず、各ドキュメントを正としてリンクします。

---

## 1. 参照ドキュメント（正となる情報源）

- 要件定義書: `requirements.md`
- 設計書: `design.md`
- 実装計画書: `implementation_plan.md`
- コード規約: `code_style.md`
- コードレビュー: `code_review.md`

このAGENTS.mdと上記ドキュメントの内容が矛盾する場合は、**上記3ドキュメントの記述を優先**し、AGENTS.mdを速やかに修正すること。AGENTS.mdは「詳細仕様」ではなく「開発運用ルール」のみを扱う。

---

## 2. プロジェクトの目的（要約）

「普通のチャット」と「プロジェクトチャット（RAG + ファイル操作）」を持つ、完全ローカル動作のmacOS向けAIアシスタント。詳細は`requirements.md`参照。

**開発者は個人開発者本人。最短でMVPをリリースし、実ユーザー（自分自身）からのフィードバックで改善するサイクルを最優先とする。** 過剰な抽象化・将来を見越した拡張性への投資は避け、`implementation_plan.md`のスコープに忠実に実装する。

---

## 3. 開発体制とエージェントの役割

| エージェント | 役割 |
|---|---|
| Codex | `implementation_plan.md`のタスク（T0-1〜T9-1）を上から順に実装 |
| Claude Code | Codexが実装した内容をレビュー。設計書・要件定義書との整合性、コード品質、セキュリティ観点をチェック |

### リポジトリ・ブランチ運用

- GitHubにリポジトリを作成し、バックアップ・履歴管理のためにリモートへpushする（Public/Privateは開発者の判断による。アプリ自体の「完全ローカル動作」要件は実行時の話であり、開発時にGitHubを使うこと自体とは無関係）
- **PR（Pull Request）は作成しない**。ブランチのマージ・レビューはPRのUIを介さず、`code_review.md`のMarkdownファイルによるレビューフローで行う
- `main`: リリースライン。**開発中は直接コミット・直接マージを禁止**する
- `develop`: 開発中の統合ブランチ。すべての機能ブランチはここから分岐し、レビュー後にここへマージする
- 機能ブランチ: 開発作業は必ず`develop`から新しいブランチを切って行う。ブランチ名は端的でわかりやすい名前にする（例: `feature/auth`, `fix/uiux`）

### 連携フロー

1. Codexは`develop`から機能ブランチ（`feature/xxx`, `fix/xxx`など）を切り、タスク（またはタスクのまとまり）ごとに実装・コミットする
2. 実装完了後、Codexは`code_review.md`の手順に従って実装サマリーmdを作成する。CodexとClaude Codeは直接連携できないため、**開発者がCodexの完了を確認した上で、Claude Codeにレビューを依頼する**（`review-codex`スキルを使う）
3. Claude Codeは`code_review.md`の手順でレビューを行い、レビュー結果mdを作成する。問題があれば開発者がCodexに修正を依頼し、レビューのサイクルを繰り返す
4. 承認されたら、機能ブランチを`develop`にマージし、GitHubへpushする（バックアップ目的。マージ自体はローカルで行う）
5. `develop` → `main`のマージは**MVP完成時（`implementation_plan.md` T9-1の統合動作確認が完了した時点）のみ**行う。それまで`main`は初期状態のまま据え置き、`develop`が実質的な作業ブランチとなる
6. コミットメッセージは `T{フェーズ}-{連番}: 内容` の形式を推奨（例: `T2-1: 普通のチャットAPI(SSE)を実装`）— タスクIDと実装計画書を対応させ、進捗を追いやすくする

> Codexが`git push`を実行するには、事前にCodex側のネットワークアクセス設定（`network_access = true`など）が必要。詳細はPhase 0の環境構築で対応する
> 上記は個人開発を前提とした推奨運用であり、運用しながら調整してよい。

---

## 4. 実装を進める上での原則

- **フェーズ順序を守る**: `implementation_plan.md`の依存関係（タスク依存関係サマリー）を飛ばさない。並行可と明記されたタスク（Phase 8など）以外は依存タスク完了後に着手する
- **スコープ外機能に手を出さない**: `requirements.md`「将来的な機能（スコープ外）」に挙げられた機能（Windows/Linux対応、ファイルプレビュー、複数チャットセッション管理など）はMVPでは実装しない
- **テストコードは書かない**: `implementation_plan.md`の方針通り、MVPでは動作確認のみ。ただし関数の責務は明確に分離し、後からテストを追加しやすい設計にする
- **迷ったら設計書を正とする**: UIレイアウト・API仕様・データ設計で判断に迷った場合は`design.md`の記述に従う。書かれていない場合はCodexが妥当な実装を行い、Claude Codeがレビュー時に指摘する

---

## 5. コード規約

詳細は `code_style.md` を参照。フロントエンド（TypeScript/React）・バックエンド（Python/FastAPI）・共通のコーディングルールをまとめている。

型チェック・Lintの方針は「バランス型」（安全性の最低限は維持しつつ、Lint/型エラーではコミット・マージをブロックしない）。詳細な項目・具体例は`code_style.md`側で管理し、AGENTS.mdでは重複記載しない。

---

## 6. 使用プラグイン（MCP）

| プラグイン      | 用途                                                                                        | 対応エージェント            |
| ---------- | ----------------------------------------------------------------------------------------- | ------------------- |
| Context7   | ライブラリ・フレームワークの最新ドキュメント参照（バージョン差異による誤実装を防ぐ）                                                | Codex / Claude Code |
| shadcn MCP | `design.md`で採用しているshadcn/ui + Tailwindのコンポーネント・ブロックをレジストリから検索・自然言語でインストール。フロントエンドUI実装時に使用 | Codex               |

- 新しいライブラリ・API仕様を扱う実装では、必ずContext7で最新ドキュメントを確認してから実装する（学習データの古い情報での誤実装を防ぐ）
- フロントエンドのUI実装（コンポーネント追加・レイアウト構築）ではshadcn MCPを優先的に使い、手書きでのコンポーネント自作は最小限にする
- Codex側のMCP設定は`~/.codex/config.toml`に手動で追加する（shadcn CLIからの自動設定は非対応）

---

## 7. アーキテクチャ上、常に守るべき制約

以下は`design.md`の設計方針の中でも、実装中に逸脱しやすい重要な制約。Claude Codeは特にこれらの逸脱を重点的にレビューすること。

- React → FastAPIは**直接HTTP通信（axios）**。Tauriの`invoke()`は使わない
- Tauriの役割は**ウィンドウ管理とFastAPIサイドカーの起動・終了のみ**。ビジネスロジックをRust側に書かない
- LLM推論・EmbeddingはすべてOllama経由。Ollama以外の外部LLM APIを呼び出さない（オフライン要件）
- ファイル操作は、**新規作成を対象プロジェクトフォルダ直下に限定**し、既存ファイルの編集は対象プロジェクトフォルダ配下に限定する。`filename`にパストラバーサル文字列（`../`など）が含まれる場合は書き込みを拒否し、シンボリックリンク経由でプロジェクト外を変更しないこと（オフラインアシスタントがファイル書き込み権限を持つ以上、必須の安全策として扱う）
- 確認モード/自走モードの切り替えは、ファイル書き込みの前段にあるゲートであり、この分岐を迂回する実装をしない

---

## 8. コードレビュー

詳細は `code_review.md` を参照。「コードレビュー基準（何を見るか）」と「コードレビューの進め方（`docs/reviews/`配下のmdファイルによるレビューサイクル）」をまとめている。

GitHub PRは使わず、Markdownファイルでレビューのやり取りを記録する。CodexとClaude Codeは直接連携できないため、各ステップの受け渡しは開発者が行う（詳細は`code_review.md`）。

---

## 9. セキュリティ・プライバシー原則

- 完全ローカル動作が絶対要件。新規に外部サーバーへの通信を追加しない（`requirements.md` 6章）
- ユーザーのMarkdown/テキストファイルの内容を外部に送信する処理を追加しない
- 自走モードでの即時書き込みは、ファイル新規作成・編集の範囲に限定する。ファイル削除など、より破壊的な操作を自走モードに追加する場合は要件定義書の更新とユーザー確認を挟む

---

## 10. AIに実行してほしくないコマンド・操作

以下は、**アプリのコード（API/サービス層）を経由せず、AIが手元で直接実行する操作**を対象とした禁止事項。実装計画書に基づき通常のコードとして書く分には対象外（詳細は各項目の注記を参照）。

**Git関連**
- `main`ブランチへの直接コミット・直接push・直接マージ（3章のブランチ運用と矛盾するため）
- `git push --force`（自分の作業中の機能ブランチを取り消す目的を除く。`main`・`develop`には行わない）
- `git reset --hard`（共有ブランチ上での実行）
- `main`・`develop`ブランチの削除
- `code_review.md`のレビューで`要修正`のまま、またはレビュー未実施の変更を自己判断で`develop`にマージすること

**ファイルシステム関連**
- `rm -rf`など、確認なしの一括削除（対象がプロジェクトフォルダ外、またはユーザーの意図が不明な場合）
- プロジェクトフォルダ（RAG対象フォルダ）外へのファイル書き込み・削除
- OS標準ディレクトリやdotfiles（`~/.ssh`, `~/.zshrc`など）の変更

**データベース関連**
- ターミナル等から`sqlite3 chat.db "DROP TABLE ..."`のように、アプリのコードを経由せず直接SQLiteファイルを操作すること
- デバッグ目的で`rm chat.db`のようにDBファイルそのものを削除すること
- `chroma_client.delete_collection()`等を、アプリのAPIフロー（`DELETE /projects/{id}`）を経由せず単独のスクリプト・シェルから直接呼び出すこと
- 対象外（問題なし）: `backend/db/models.py`でのテーブル定義、`Base.metadata.create_all()`等によるDB初期化コード、`backend/services/rag.py`でのChromaDBコレクション作成コードなど、実装計画書に基づき通常のアプリケーションコードとして書く処理

**Ollama関連**
- `ollama rm`（モデル削除）をユーザー確認なく実行すること

**ネットワーク関連**
- Ollama以外の外部サーバーへの通信（`curl`/`wget`等での未知のホストへのアクセス。オフライン要件に反するため）
- ※GitHubへの`git push`（バックアップ・履歴管理目的）など、開発フローのための通信はこの禁止事項の対象外。あくまで**アプリのランタイムコードが外部と通信すること**を禁止する項目であり、開発ツール自体の通信とは区別する

**権限・環境関連**
- `sudo`を伴うコマンド全般
- 環境変数・APIキーなど秘密情報をログやコミットに出力する操作

---

## 11. ドキュメント更新ルール

- 実装中に設計・仕様を変更した場合は、`design.md`または`implementation_plan.md`を更新すること（AGENTS.mdへの反映は運用ルールに影響する場合のみ）
- 「その他の設計決定事項」（`design.md` 8章）に類する未決定事項が新たに発生した場合は、同様の表形式で追記する
